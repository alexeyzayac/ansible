---

- name: Установить инструменты разработки
  ansible.builtin.package:
    name:
      - build-essential
      - gcc
      - make
      - cmake
      - python3
      - python3-pip
      - nodejs
      - npm
      - yamllint
    state: latest
  when: ansible_facts['os_family'] == "Debian"

...